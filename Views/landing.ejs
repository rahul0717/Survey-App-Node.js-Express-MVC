<!DOCTYPE html>
<html lang="eng">

<head>
    <title>SER421 MVC Ex Landing Page</title>
</head>

<body>
    <p><em>SER421 MVC Ex Landing Page</em></p>
    <form action="/landing" method="POST">
        <label for="username">Username:</label>
        <% if (username !=null) { %>
        <input type="text" name="usernameTxt" required value=<%= username %> pattern="^[a-zA-Z]+$"
            title="Only Lower or Upper case letters without space Allowed" >
        <% } else{ %>
        <input type="text" name="usernameTxt" required pattern="^[a-zA-Z]+$"
            title="Only Lower or Upper case letters without space Allowed">
        <% } %>
        <br>
        <input type="submit" name="action" value="survey">
        <input type="submit" name="action" value="match">



    </form>

</body>

</html>